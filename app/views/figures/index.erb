<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Historical Figures</title>
  </head>
  <body>
    <h1>Historical Figures</h1>
      <!-- figure[name] -->
      <h2>Figure Information</h2>
      <% @figures.each do |figure| %>
        <h3><a href="/figures/<%=figure.id%>"<h3>Name: <%= figure.name %></a></h3>
        <!-- figure[title_ids][] -->
        <h3>Titles:</h3>
          <ul>
          <% figure.titles.each do |title| %>
            <li> <%= title.name %></li>
          <% end %>
          </ul>
     

        <!-- figure[landmark_ids][] -->
        <h3>Landmarks:</h3>
          <ul>
          <% figure.landmarks.each do |landmark| %>
            <li> <%= landmark.name %> </li>
          <% end %>
          </ul>
      <% end %>

  </body>
</html>
